<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.himedia.board.dao.IReplyDao" >

    <select id="selectReply" resultType="com.himedia.board.dto.ReplyDto" >
        select * from reply where boardnum=#{num} order by replynum desc
    </select>

    <insert id="insert" parameterType="com.himedia.board.dto.ReplyDto" >
        insert into reply(boardnum, userid, content) values( #{boardnum}, #{userid}, #{content})
    </insert>

    <delete id="delete">
        delete from reply where replynum=#{replynum}
    </delete>

    <select id="getReplyCount" resultType="_int">
        select count(*) from reply where boardnum=#{num}
    </select>

</mapper>